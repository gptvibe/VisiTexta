<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="App_Desktop.Pages.ModelsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="28" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock Text="Models" Style="{StaticResource SectionTitleTextBlockStyle}" />
            <TextBlock Text="Curated local GGUF OCR models. Downloads are stored locally and checksum-verified when Hugging Face metadata provides hashes." Style="{StaticResource MutedTextBlockStyle}" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="CustomLocatorTextBox" Header="Advanced custom GGUF locator" PlaceholderText="owner/repo/file.gguf" />
                <Button Grid.Column="1" Content="Download" Click="DownloadCustom_Click" VerticalAlignment="Bottom" />
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}" Padding="14">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="DownloadStatusTextBlock" Text="Ready" FontWeight="SemiBold" VerticalAlignment="Center" />
                <ProgressBar x:Name="DownloadProgressBar" Grid.Column="1" Minimum="0" Maximum="100" Value="0" VerticalAlignment="Center" />
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="3">
            <StackPanel x:Name="ModelsStackPanel" Spacing="12" />
        </ScrollViewer>
    </Grid>
</Page>
